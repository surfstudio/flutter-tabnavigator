name: example
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  tabnavigator:
    path: ../

dev_dependencies:
  surf_lint_rules: ^2.0.0

flutter:
  uses-material-design: true
